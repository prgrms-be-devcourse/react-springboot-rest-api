spring:
  datasource:
    url: jdbc:h2:mem:kdt
    username: sa
    password:
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      schema-locations: classpath:h2/schema.sql